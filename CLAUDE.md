# CLAUDE.md — fixed_fund_analysis 项目规则

## 一、数据完整性（最高优先级，不可违反）

1. **禁止捏造任何金融数据**。任何净值、收益率、RBA利率等数值，必须能追溯到
   真实抓取的数据源（URL + 抓取时间）。如果数据缺失或抓取失败，必须明确报错
   并停止计算，不允许用估算值、历史平均值或"合理猜测"填补空白。
2. 任何基金的历史数据序列，最低要求 **36个月** 连续数据用于动态φ（自相关系数）
   估计，不满足则该基金标记为"数据不足"，不参与Sortino/去平滑相关计算。
3. 数据缺口（gap）零容忍：如果某月数据缺失，管道必须报错并列出缺失的具体
   月份，不允许跳过继续计算或用插值填补。

## 二、子agent委派规则

1. 涉及网络抓取（WebFetch/WebSearch）、大批量数据处理的任务，应委派给
   子agent执行，主对话只负责编排和结果解读，不直接处理原始抓取数据。
2. 子agent返回结果后，主对话需要核对返回数据是否符合"一、数据完整性"
   的要求，如有可疑（比如数值突变、格式异常），应重新委派验证而非直接采信。

## 三、代码修改与验证流程（2026-07新增）

### 3.1 修改后避免不必要的全文件重读
使用 str_replace / Edit 工具修改文件后，不要重新 view 整个文件确认结果。
只需 view 被修改的行号范围（前后各扩展5行）。只有涉及函数签名变化、
可能影响其他调用点时，才用 grep 确认下游影响。

### 3.2 修改计算逻辑代码后，先跑单元测试，再跑端到端pipeline
修改 scripts/ 目录下任何计算逻辑代码（尤其 metrics.py、registry_schema.py）后：
1. 先运行对应模块的 pytest 单元测试
2. 没有覆盖这次改动的测试，先补充测试用例再改代码
3. 单元测试全部通过后，才允许运行一次 run_all.py 做端到端验证
4. run_all.py 报错时，优先判断能否把这个报错场景补充为新的单元测试，
   而不是直接改代码后再跑一遍猜测

## 四、Pipeline 前置校验（2026-07新增）

1. run_all.py 执行的 Step 0 必须是 `scripts/validate_registry.py`，
   对 fund_registry.yaml 做 Pydantic schema 校验，格式不合规立即终止，
   不进入后续抓取/计算流程。
2. APIR代码格式：3位大写字母 + 4位数字 + AU（如 ETL5010AU），
   正则：`^[A-Z]{3}\d{4}AU$`


## 五 禁止对提取到的原始数值进行"合理性纠正"**。
   如果解析出的数据在数值上显得异常（如单月回报率远超历史正常波动范围），程序不得
   自动修改、剔除或"猜测式还原"该数值。程序唯一被允许做的是：
   如实保留原始提取值参与计算，同时生成醒目的异常标记，交由人工
   判断这是数据错误还是真实的极端市场事件。任何形式的"自动纠错"
   逻辑（哪怕理由看起来合理），都必须先经过人工确认才能写入代码，
   不能由子agent自主决定并悄悄应用。
   上述"异常值保留、交人工复核"原则，仅适用于同一字段口径下的
   数值异常（如真实单月回报出现极端值）。如果发现某个数据点的
   *字段类型本身就提取错误*（如把季度滚动回报误当作月度回报、
   把年化数值误当作月度数值），这属于数据管道的结构性缺陷，
   必须视为数据缺口处理（不参与计算），不能套用"保留原值+异常告警"
   的处理方式，因为该数值本身在维度上就是不可比的，保留下来
   会污染而非提示风险。
## 六、 历史教训：防范大模型幻觉回填（2026-07新增）
1. **背景**：在 `213bdd` 提交中，大模型为了让 Stake 基金的累计收益对齐官方发布的“自成立以来年化 5.93%”，违规在 `parse_factsheet.py` 中硬编码了 3 个月的常数 `0.00657` 来回填数据缺口。
2. **根因防范**：大模型往往会由于追求逻辑自洽（消除数据 gap 或匹配终值）而产生“合理幻觉”并篡改基础数据。
3. **架构约束**：
   - 现已在 `validate_data.py` 中加入 `ANTI-FABRICATION GUARD`，严禁连续出现相同的精确浮点数插值。
   - 任何涉及补齐（backfill）、前推（forward-fill）的逻辑，在数据摄取层（Raw -> JSON）被绝对禁止。提取层只能做纯文本到数字的映射。
   - 如果遇到基金成立初期无披露月报的情况，直接把数据序列起点设为**第一份真实研报的日期**，由下游的 `metrics.py` 根据可用月数动态处理，绝不允许反推捏造填补。
