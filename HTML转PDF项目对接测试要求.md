# HTML 转 PDF 方案选型测试要求

## 背景

有一类 HTML 报告页面，需要转换成 PDF 后交给下游做文字提取（下游用 LLM 读 PDF
拿数值）。之前的做法是直接对 HTML 原始文本做字节级切片（定位关键字、截取前后
一段窗口）喂给 LLM，结果出过真实事故：页面里有两处视觉上互不相关、但文本距离
很近的数值区块（一张"当前数据表"和一张"历史统计小卡片"），字节切片窗口把两者
都框了进去，LLM 认错了行，把不该用的数字当成了答案。

根因是：纯文本字节距离不等于页面视觉/版面结构上的"这几个数字属于同一张表"——
表格、多栏布局、图表这些结构信息，在把 HTML 打散成一维字节流的那一刻就已经丢失，
靠正则锚点/窗口大小去猜，会不断复现同类错误。

**结论**：正确的修法是代码层硬约束，不是靠 prompt 里堆"如果对不上就别用"这类
指令（prompt 是软约束，模型不一定听）。硬约束方案 = 把 HTML 转成 PDF，交给
现成的、按页读版面的 PDF 提取通路（HTML 转 PDF 这一步纯代码/浏览器自动化完成，
不引入新的模型判断），版面结构在 PDF 里天然保留，"认错表格"这类错误从代码结构上
就不可能再发生。

## 已知的一个真实拦路虎（必须在测试里覆盖，不能回避）

目标页面里除了常规表格/文字内容，还内嵌了一个 Plotly.js 交互图表，图表上的逐月
数值只存在于 **hover-only 的 tooltip 文本**里（`hoverinfo:"text"`，鼠标悬停才
显示，静态渲染/打印不会画出来）。

实测验证过：用 Playwright headless Chromium 对这个页面直接 `page.pdf()` 打印导出，
生成的 PDF 里**完全读不到**任何一条历史数值（hover 专属文本，转出来的 PDF 文本层
一个字都没有）——因为浏览器打印/PDF 导出不会主动触发 hover 交互，这些数字在
"打印视图"里从未被渲染过。

**这是本次要测试的核心难点**：候选方案必须能处理"数据只存在于交互态、不在默认
静态渲染里"的情况，不能只是处理普通静态 HTML（普通表格/文字随便什么方案都能转好，
不是本次测试重点）。

## 希望的工作流程

1. **先去网络搜索**：调研业界怎么解决"HTML(含交互图表)转PDF不丢数据"这个问题，
   在 GitHub 上找相关开源项目/工具（关键词参考：headless browser hover
   automation to pdf、plotly export static image with labels、html to pdf
   preserve tooltip data、puppeteer/playwright chart export 等，不限于这些）。
2. **列一份待测清单**：把调研到的候选方案（项目名 + GitHub 链接 + 核心原理
   一句话）列出来，按"看起来最可能解决 hover-only 数据丢失问题"排优先级。
3. **实际去转**：拿下面的测试用例逐个跑通候选方案，产出转换后的 PDF 文件
   （或图片形式的页面也可以，只要视觉上数字清晰可读）。**不需要你自己判断
   转换结果对不对**——把每个候选方案转出来的 PDF/截图产物保留好路径，我会
   自己去核对是否正确。
4. **整理报告**：把每个候选方案的测试结果（成功/失败/部分成功）+ 最终推荐
   哪一个 + 具体怎么对接（依赖什么库/工具、调用方式、需要多少代码量）整理成
   报告给我。

## 测试目标（候选方案需满足）

1. **保留所有正常可见内容的文字/表格结构**（基线要求，大概率都能过）。
2. **能捕获 hover-only / 交互态才显示的内容**（核心难点）。可能的实现思路
   （不限于，欢迎调研到其他思路）：
   - 用 headless 浏览器自动化模拟对图表每个数据点做 hover（或触发等价 JS
     事件），把每次 hover 出现的 tooltip 文本抓下来，固化成静态可见内容
     一并放进最终 PDF。
   - 直接从页面 DOM/JS 变量里把底层数据（图表库的原始 data 数组，不是渲染
     结果）结构化提取出来，重新排版生成一份可读 PDF。
   - 无头浏览器截图方式：对每个数据点"移动鼠标 -> 截图"，把截图按顺序拼进
     PDF（图片形式即可，不强制要求文字层，只要视觉可读）。
3. **不能捏造/推断数据**：转换过程必须是对页面里已存在的真实数据做格式转写
   （截图/重新排版/固化交互态），绝不允许对读不到的数据做估算、插值、猜测性
   填充。任何方案如果涉及"用模型生成/推测缺失内容"都不可接受。
4. **确定性、可重复**：同一个输入页面，多次运行结果应该一致，不能这次转出来
   有数据、下次没有。
5. **转换步骤本身不依赖 LLM 做判断**：应该是纯代码/浏览器自动化流程，不要在
   "HTML 怎么转 PDF"这一步引入新的模型决策点。

## 测试用例（必须用这个真实页面验证，不能只用简单 demo 页）

- 目标 URL: `https://coolabahcapital.com/performance-report-coolabah-floating-rate-high-yield-fund-i`
  （页面约 16.9MB，Plotly hover-only 数据图表 + 常规表格 + 文字段落 + 大量内联
  base64 图片）
- 转换完成后检查最终产物（PDF文本层，或图片形式则做 OCR/人工核对）是否包含
  以下真实数值（已从原始 HTML 源码核实过，可用来做断言）：
  - `2022-12-31: $100.98`
  - `2023-04-30: $104.74`
  - `2026-06-30: $136.19`
  - 以及页面里那张常规表格（"Period Ending 2026-06-30" / "1 month 0.78%
    0.71%" 那张表）应该完整保留

## 交付物要求

1. **待测清单**：调研到的候选方案列表（名称+GitHub链接+原理简述+优先级）。
2. **逐个测试结果**：每个候选方案是否跑通、产出物路径、遇到的问题（超时/
   报错/部分数据缺失等如实说明，不要为了显得"方案可行"回避负面结果）。
3. **最终推荐**：哪个方案最合适，为什么。
4. **集成说明**：需要哪些依赖（Python包/系统二进制/浏览器驱动等）、大概的
   调用方式/代码量、跟现有 Playwright 依赖能否复用。

## 不要做的事

- 不要只用简单静态 demo 页验证——必须用上面给的真实 URL，问题核心就在于这个
  页面的复杂度（hover-only 数据 + 巨量内联图片）。
- 不要只检查部分数据（比如只验证常规表格转过去了，不检查 hover-only 历史数据
  是否也转过去了）——两类内容都要验证并在报告里分别说明。
- 不要引入需要联网调用第三方付费 API 的转换方案——转换步骤本身应该是本地可跑
  的开源工具/浏览器自动化，不是"调另一个云端转换服务"。
- 不要自己判断转换结果"看起来对不对"就下结论——把产物留痕给出路径，最终对不
  对由我来核实。
